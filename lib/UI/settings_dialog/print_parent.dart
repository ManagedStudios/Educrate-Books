import 'package:flutter/material.dart';

class PrintParent extends StatelessWidget {
  const PrintParent({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Hi I'm Drucken")
      ],
    );
  }
}

import 'package:flutter/material.dart';

class FilterParent extends StatelessWidget {
  const FilterParent({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Hi I'm Filter")
      ],
    );
  }
}

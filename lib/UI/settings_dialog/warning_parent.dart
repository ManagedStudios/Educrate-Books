import 'package:flutter/material.dart';

class WarningParent extends StatelessWidget {
  const WarningParent({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Hi I'm Mahnung")
      ],
    );
  }
}
